<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">
<mapper namespace="com.kh.auction.user.dao.AuctionDAO">

	<select id="getAllOngingAuctionNum">
		select count(*)
			from auction
		    join (select *
		    		from consignment 
		    			join attachment on (con_no = att_bno)
		    		where att_category = 2 and att_fno = 1) on (auc_cno = con_no)
		where sysdate between auc_start_date and auc_finish_date
	</select>

	<select id="getAllAuction" resultType="Auction">
		<![CDATA[
		select auc_no, auc_start_price, auc_finish_price ,to_char(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') as auc_start_date, 
				to_char(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') as auc_finish_date, auc_mem_id, con_author, con_product, con_width, con_height,
				con_year, con_etc, att_rename
		from auction
		    join (select *
		    		from consignment 
		    			join attachment on (con_no = att_bno)
		    		where att_category = 2 and att_fno = 1) on (auc_cno = con_no)
		where sysdate between auc_start_date and auc_finish_date
        order by auc_no desc
		]]>
	</select>
	
	<select id="getAuctionDetail" resultType="Auction">
		select auc_no, auc_start_price, auc_finish_price, to_char(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') as auc_start_date, 
				to_char(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') as auc_finish_date, auc_mem_id, con_author, con_product, con_width, con_height,
				con_year, con_etc, att_rename
		from auction
		    join (select *
		    		from consignment 
		    			join attachment on (con_no = att_bno) 
		    		where att_category = 2 and att_fno = 1) on (auc_cno = con_no)
		where auc_no = #{aucNo}
	</select>
	
	<select id="getAuctionAttachment" resultType="Attachment">
		select *
		from attachment
		where att_bno = (select auc_cno
                        from auction
                        where auc_no = #{aucNo}) and att_status = 'Y' and att_category = 2 and (att_fno = 2 or att_fno = 3)
		order by att_fno
	</select>
	
	<update id="insertBid">
		update auction
		set auc_finish_price = #{bidMoney}, auc_mem_id = #{id}
		<![CDATA[
		where (auc_finish_price < #{bidMoney} or auc_finish_price < #{bidMoney}) and auc_no = #{aucNo}
		]]>
	</update>

	<update id="returnUnsold">
		update member
		set mem_balance = mem_balance + #{beforeBidMoney}
		where mem_id = #{beforeId}
	</update>
	
	<update id="updateMoneyInsertBid">
		update member
		set mem_balance = mem_balance - #{bidMoney}
		where mem_id = #{id}
	</update>
	
	<insert id="insertBiddingDetail">
		insert into bidding_Detail values(bdd_seq.nextval, #{bidMoney}, #{aucNo}, #{id})
	</insert>
	
	<select id="checkLike" resultType="int">
		select count(*)
		from bidding_interest
		where mem_id = #{id} and auc_no in
		 <foreach collection="aucNo" item="aucNo" open="(" separator="," close=")">
			#{aucNo}
		</foreach> 	
	</select>
	
	<insert id="insertLike">
		insert into bidding_interest values 
		<foreach collection="aucNo" item="aucNo" open="(" close="," >
        	#{aucNo}
    	</foreach>
    	 #{id})
	</insert>
	
	<delete id="deleteLike" parameterType="java.util.Map">
		delete from bidding_interest 
		where mem_id = #{id} and auc_no in
		<foreach collection="aucNo" item="aucNo" open="(" separator="," close=")">
			#{aucNo}
		</foreach> 
	</delete> 
	
	<select id="getMyInterestList">
		select a.auc_No, att_Rename, con_Author, con_Product, to_char(auc_start_Date, 'YYYY-MM-DD HH24:MI:SS') as auc_start_Date, to_char(auc_Finish_Date, 'YYYY-MM-DD HH24:MI:SS') as auc_Finish_Date, auc_Finish_Price
		from bidding_interest b
			join auction a on (a.auc_no = b.auc_no)
            join consignment on (a.auc_cno = con_no)
            join attachment p on (auc_cno = att_bno)
        where b.mem_id = #{id} and att_fno = 0
		<if test="type != null and type == 'scheduled'">
			and auc_start_date > sysdate 
		</if>
		<if test="type != null and type == 'ongoing'">
			and sysdate between auc_start_date and auc_finish_date 
		</if>
		<if test="type != null and type == 'end'">
			and auc_finish_date <![CDATA[<]]> sysdate 
		</if>
	</select>
	
	<select id="getAllInterestBidNum" resultType="int">
		select count(*)
		from bidding_interest b
			join auction a on (a.auc_no = b.auc_no)
		where mem_id = #{id}
		<if test="type != null and type == 'scheduled'">
			and auc_start_date > sysdate 
		</if>
		<if test="type != null and type == 'ongoing'">
			and sysdate between auc_start_date and auc_finish_date 
		</if>
		<if test="type != null and type == 'end'">
			and auc_finish_date <![CDATA[<]]> sysdate 
		</if> 
	</select>
	
	
	
	
	
	<select id="getAllMyBidListCount" resultType="int">
		select count(*)
		from bidding_detail b
			join auction a on (a.auc_no = b.auc_no)
		where mem_id = #{id}
		<if test="type != null and type == ongoing">
			and sysdate between auc_start_date and auc_finish_date 
		</if>
		<if test="type != null and type == end">
			and auc_finish_date <![CDATA[<]]> sysdate 
		</if>
	</select>
	
	<select id="getAllMyBidList" resultType="Auction">
		select a.auc_no, auc_start_price, auc_finish_price, to_char(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') as auc_start_date, 
				to_char(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') as auc_finish_date, auc_mem_id, con_author, con_product, con_width, con_height,
				con_year, con_etc, att_rename
		from auction a
		    join (select *
		          from consignment 
		            join attachment on (con_no = att_bno)
		          where att_category = 2 and att_fno = 1) on (auc_cno = con_no)
		    join bidding_detail b on (a.auc_no = b.auc_no)
		where a.auc_no in (select b.auc_no
		                 	from bidding_detail
		                 	where b.mem_id = #{id}
		                  )
		<if test="type != null and type == ongoing">
			and sysdate between auc_start_date and auc_finish_date 
		</if>
		<if test="type != null and type == end">
			and auc_finish_date <![CDATA[<]]> sysdate 
		</if>                  
		order by bid_no desc	
	</select> 
	
	<select id="getAllMyDetail">
		select *
		from bidding_detail b
			join auction a on (a.auc_no = b.auc_no)
		where mem_id = #{id}
		<if test="type != null and type == ongoing">
			and sysdate between auc_start_date and auc_finish_date 
		</if>
		<if test="type != null and type == end">
			and auc_finish_date <![CDATA[<]]> sysdate 
		</if>
		order by bid_no desc
	</select>
	
	<!-- adimin -->
	<!-- adimin -->
	<!-- adimin -->
	<!-- adimin -->
	<!-- adimin -->
	<!-- adimin -->
	<!-- adimin -->
	<!-- adimin -->
	<!-- adimin -->
	<!-- adimin -->
	<!-- adimin -->
	
	<select id="getAdminSearchListCount" parameterType="java.util.Map">
    SELECT COUNT(*)
    FROM auction
    JOIN (
        SELECT *
        FROM consignment
        JOIN attachment ON (con_no = att_bno)
        WHERE att_category = 2 AND att_fno = 1
    ) ON (auc_cno = con_no)
    	<choose>
    		<when test="search == 'all'">
    			where con_product like '%' ||  #{content} || '%' or con_author like  '%' ||  #{content} || '%' and ( 1=1 
			    	<if test="scheduled != null">
			    		or (auc_start_date > sysdate
			    			<if test="aucSS != null">
					    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucSE != null">
					    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucFS != null">
					    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	<if test="aucFE != null">
					    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	)  
			    	</if>
			    	<if test="ongoing != null">
			   			or (sysdate between auc_start_date and auc_finish_date
			   			<if test="aucSS != null">
			    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucSE != null">
			    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucFS != null">
			    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	<if test="aucFE != null">
			    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	)
			    	</if>
			    	<if test="end != null">
			    		or (sysdate > auc_finish_date
			    		<if test="aucSS != null">
				    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucSE != null">
				    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucFS != null">
				    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	<if test="aucFE != null">
				    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	)
			    	</if>
			    	)
    		</when>
    		
    		<when test="search == 'author'">
    			where con_author like  '%' ||  #{content} || '%' and ( 1=1 
			    	<if test="scheduled != null">
			    		or (auc_start_date > sysdate
			    			<if test="aucSS != null">
					    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucSE != null">
					    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucFS != null">
					    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	<if test="aucFE != null">
					    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	)  
			    	</if>
			    	<if test="ongoing != null">
			   			or (sysdate between auc_start_date and auc_finish_date
			   			<if test="aucSS != null">
			    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucSE != null">
			    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucFS != null">
			    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	<if test="aucFE != null">
			    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	)
			    	</if>
			    	<if test="end != null">
			    		or (sysdate > auc_finish_date
			    		<if test="aucSS != null">
				    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucSE != null">
				    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucFS != null">
				    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	<if test="aucFE != null">
				    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	)
			    	</if>
			    	) 
    		</when>
    		<when test="search == 'work'">
    			where con_product like '%' ||  #{content} || '%'  and ( 1=1 
			    	<if test="scheduled != null">
			    		or (auc_start_date > sysdate
			    			<if test="aucSS != null">
					    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucSE != null">
					    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucFS != null">
					    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	<if test="aucFE != null">
					    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	)  
			    	</if>
			    	<if test="ongoing != null">
			   			or (sysdate between auc_start_date and auc_finish_date
			   			<if test="aucSS != null">
			    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucSE != null">
			    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucFS != null">
			    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	<if test="aucFE != null">
			    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	)
			    	</if>
			    	<if test="end != null">
			    		or (sysdate > auc_finish_date
			    		<if test="aucSS != null">
				    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucSE != null">
				    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucFS != null">
				    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	<if test="aucFE != null">
				    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	)
			    	</if>
			    	)
    		</when>
    	</choose>
	</select>    	
    	<!-- <if test="aucSS != null">
    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
    	</if>
    	<if test="aucSE != null">
    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
    	</if>
    	<if test="aucFS != null">
    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
    	</if>
    	<if test="aucFE != null">
    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
    	</if> -->
    	
    	
    	
    	<!-- <choose>
    		<when test="scheduled == 'scheduled'">
    			and (auc_start_date > sysdate
    			 <choose>
	        x만 존재 (시작일만 주어짐): x
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y 존재 (시작일 범위 주어짐): x, y
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z 존재 (시작일, 종료일 범위 주어짐): x, z
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE == null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, a 존재 (시작일, 종료일 범위 주어짐): x, a
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE != null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y만 존재 (시작일만 주어짐): y
	        <when test="aucSS == null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z 존재 (시작일 범위 주어짐): y, z
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        y, a 존재 (시작일 범위 주어짐): y, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z만 존재 (종료일만 주어짐): z
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE == null">
	           and (auc_finish_date aucFS <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z, a 존재 (시작일, 종료일 범위 주어짐): z, a
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE != null">
	           and (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	        a만 존재 (종료일만 주어짐): a
	        <when test="aucSS == null and aucSE == null and aucFS == null and aucFE != null">
	           and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z 존재 (시작일, 종료일 범위 주어짐): x, y, z
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE == null">
	           and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, y, a 존재 (시작일, 종료일 범위 주어짐): x, y, a
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE != null">
	          and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z, a 존재 (시작일, 종료일 범위 주어짐): x, z, a
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE != null">
	            and((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z, a 존재 (시작일, 종료일 범위 주어짐): y, z, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE != null">
	            and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            AND auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z, a 존재 (시작일, 종료일 범위 주어짐): x, y, z, a
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE != null">
	           and((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
			<otherwise></otherwise>
	        주어진 날짜 없음: (모든 날짜 범위가 주어지지 않음)
	    </choose>
	    	) 
    		</when>
    		<when test="ongoing == 'ongoing'">
    			and (sysdate between auc_start_date and auc_finish_date
    			<choose>
	        x만 존재 (시작일만 주어짐): x
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y 존재 (시작일 범위 주어짐): x, y
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z 존재 (시작일, 종료일 범위 주어짐): x, z
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE == null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, a 존재 (시작일, 종료일 범위 주어짐): x, a
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE != null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y만 존재 (시작일만 주어짐): y
	        <when test="aucSS == null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z 존재 (시작일 범위 주어짐): y, z
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        y, a 존재 (시작일 범위 주어짐): y, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z만 존재 (종료일만 주어짐): z
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE == null">
	           and (auc_finish_date aucFS <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z, a 존재 (시작일, 종료일 범위 주어짐): z, a
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE != null">
	           and (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	        a만 존재 (종료일만 주어짐): a
	        <when test="aucSS == null and aucSE == null and aucFS == null and aucFE != null">
	           and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z 존재 (시작일, 종료일 범위 주어짐): x, y, z
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE == null">
	           and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, y, a 존재 (시작일, 종료일 범위 주어짐): x, y, a
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE != null">
	          and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z, a 존재 (시작일, 종료일 범위 주어짐): x, z, a
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE != null">
	            and((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z, a 존재 (시작일, 종료일 범위 주어짐): y, z, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE != null">
	            and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            AND auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z, a 존재 (시작일, 종료일 범위 주어짐): x, y, z, a
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE != null">
	           and((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
			<otherwise></otherwise>
	        주어진 날짜 없음: (모든 날짜 범위가 주어지지 않음)
	    </choose>
	    	)  
    		</when>
    		<when test="end == 'end'">
    			and (sysdate > auc_finish_date
    			<choose>
	        x만 존재 (시작일만 주어짐): x
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y 존재 (시작일 범위 주어짐): x, y
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z 존재 (시작일, 종료일 범위 주어짐): x, z
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE == null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, a 존재 (시작일, 종료일 범위 주어짐): x, a
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE != null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y만 존재 (시작일만 주어짐): y
	        <when test="aucSS == null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z 존재 (시작일 범위 주어짐): y, z
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        y, a 존재 (시작일 범위 주어짐): y, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z만 존재 (종료일만 주어짐): z
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE == null">
	           and (auc_finish_date aucFS <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z, a 존재 (시작일, 종료일 범위 주어짐): z, a
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE != null">
	           and (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	        a만 존재 (종료일만 주어짐): a
	        <when test="aucSS == null and aucSE == null and aucFS == null and aucFE != null">
	           and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z 존재 (시작일, 종료일 범위 주어짐): x, y, z
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE == null">
	           and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, y, a 존재 (시작일, 종료일 범위 주어짐): x, y, a
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE != null">
	          and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z, a 존재 (시작일, 종료일 범위 주어짐): x, z, a
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE != null">
	            and((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z, a 존재 (시작일, 종료일 범위 주어짐): y, z, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE != null">
	            and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            AND auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z, a 존재 (시작일, 종료일 범위 주어짐): x, y, z, a
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE != null">
	           and((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
			<otherwise></otherwise>
	        주어진 날짜 없음: (모든 날짜 범위가 주어지지 않음)
	    </choose>
	    	)  
    		</when>
    	</choose> -->
	

	
	<!--  -->
	<select id="getAdminSearchList" parameterType="java.util.Map">
    SELECT auc_no, auc_start_price, auc_finish_price ,to_char(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') as auc_start_date, 
				to_char(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') as auc_finish_date, auc_mem_id, con_author, con_product, con_width, con_height,
				con_year, con_etc, att_rename
    FROM auction
    JOIN (
        SELECT *
        FROM consignment
        JOIN attachment ON (con_no = att_bno)
        WHERE att_category = 2 AND att_fno = 1
    	) ON (auc_cno = con_no)
    	<choose>
    		<when test="search == 'all'">
    			where (con_product like '%' ||  #{content} || '%' or con_author like  '%' ||  #{content} || '%')
			    	<if test="scheduled != null">
			    		and (auc_start_date > sysdate
			    			<if test="aucSS != null">
					    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucSE != null">	
					    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucFS != null">
					    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	<if test="aucFE != null">
					    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	)  
			    	</if>
			    	<if test="ongoing != null">
			   			or (sysdate between auc_start_date and auc_finish_date
			   			<if test="aucSS != null">
			    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucSE != null">
			    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucFS != null">
			    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	<if test="aucFE != null">
			    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	)
			    	</if>
			    	<if test="end != null">
			    		or (sysdate > auc_finish_date
			    		<if test="aucSS != null">
				    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucSE != null">
				    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucFS != null">
				    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	<if test="aucFE != null">
				    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	)
			    	</if>
			    	)
    		</when>
    		
    		<when test="search == 'author'">
    			where (con_author like  '%' ||  #{content} || '%') and ( 1=1 
			    	<if test="scheduled != null">
			    		or (auc_start_date > sysdate
			    			<if test="aucSS != null">
					    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucSE != null">
					    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucFS != null">
					    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	<if test="aucFE != null">
					    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	)  
			    	</if>
			    	<if test="ongoing != null">
			   			or (sysdate between auc_start_date and auc_finish_date
			   			<if test="aucSS != null">
			    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucSE != null">
			    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucFS != null">
			    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	<if test="aucFE != null">
			    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	)
			    	</if>
			    	<if test="end != null">
			    		or (sysdate > auc_finish_date
			    		<if test="aucSS != null">
				    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucSE != null">
				    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucFS != null">
				    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	<if test="aucFE != null">
				    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	)
			    	</if>
			    	) 
    		</when>
    		<when test="search == 'work'">
    			where (con_product like '%' ||  #{content} || '%')  and ( 1=1 
			    	<if test="scheduled != null">
			    		or (auc_start_date > sysdate
			    			<if test="aucSS != null">
					    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucSE != null">
					    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
					    	</if>
					    	<if test="aucFS != null">
					    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	<if test="aucFE != null">
					    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
					    	</if>
					    	)  
			    	</if>
			    	<if test="ongoing != null">
			   			or (sysdate between auc_start_date and auc_finish_date
			   			<if test="aucSS != null">
			    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucSE != null">
			    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
			    	</if>
			    	<if test="aucFS != null">
			    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	<if test="aucFE != null">
			    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
			    	</if>
			    	)
			    	</if>
			    	<if test="end != null">
			    		or (sysdate > auc_finish_date
			    		<if test="aucSS != null">
				    		and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucSE != null">
				    		and (auc_start_date <![CDATA[<=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
				    	</if>
				    	<if test="aucFS != null">
				    		and (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	<if test="aucFE != null">
				    		and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
				    	</if>
				    	)
			    	</if>
			    	)
    		</when>
    	</choose>
    </select>
    	<!-- <choose>
    		<when test="search == 'all'">
    			where con_product like '%' ||  #{content} || '%' or con_author like  '%' ||  #{content} || '%' 
    		</when>
    		<when test="search == 'author'">
    			where con_author like  '%' ||  #{content} || '%'
    		</when>
    		<when test="search == 'work'">
    			where con_product like '%' ||  #{content} || '%'
    		</when>
    	</choose>
    	<choose>
    		<when test="scheduled == 'scheduled'">
    			and (auc_start_date > sysdate
    			<choose>
	        x만 존재 (시작일만 주어짐): x
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y 존재 (시작일 범위 주어짐): x, y
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z 존재 (시작일, 종료일 범위 주어짐): x, z
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE == null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, a 존재 (시작일, 종료일 범위 주어짐): x, a
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE != null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y만 존재 (시작일만 주어짐): y
	        <when test="aucSS == null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z 존재 (시작일 범위 주어짐): y, z
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        y, a 존재 (시작일 범위 주어짐): y, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z만 존재 (종료일만 주어짐): z
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE == null">
	           and (auc_finish_date aucFS <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z, a 존재 (시작일, 종료일 범위 주어짐): z, a
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE != null">
	           and (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	        a만 존재 (종료일만 주어짐): a
	        <when test="aucSS == null and aucSE == null and aucFS == null and aucFE != null">
	           and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z 존재 (시작일, 종료일 범위 주어짐): x, y, z
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE == null">
	           and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, y, a 존재 (시작일, 종료일 범위 주어짐): x, y, a
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE != null">
	          and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z, a 존재 (시작일, 종료일 범위 주어짐): x, z, a
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE != null">
	            and((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z, a 존재 (시작일, 종료일 범위 주어짐): y, z, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE != null">
	            and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            AND auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z, a 존재 (시작일, 종료일 범위 주어짐): x, y, z, a
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE != null">
	           and((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
			<otherwise></otherwise>
	        주어진 날짜 없음: (모든 날짜 범위가 주어지지 않음)
	    </choose>
	    	)  
    		</when>
    		<when test="ongoing == 'ongoing'">
    			and (sysdate between auc_start_date and auc_finish_date
    			<choose>
	        x만 존재 (시작일만 주어짐): x
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y 존재 (시작일 범위 주어짐): x, y
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z 존재 (시작일, 종료일 범위 주어짐): x, z
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE == null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, a 존재 (시작일, 종료일 범위 주어짐): x, a
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE != null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y만 존재 (시작일만 주어짐): y
	        <when test="aucSS == null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z 존재 (시작일 범위 주어짐): y, z
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        y, a 존재 (시작일 범위 주어짐): y, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z만 존재 (종료일만 주어짐): z
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE == null">
	           and (auc_finish_date aucFS <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z, a 존재 (시작일, 종료일 범위 주어짐): z, a
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE != null">
	           and (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	        a만 존재 (종료일만 주어짐): a
	        <when test="aucSS == null and aucSE == null and aucFS == null and aucFE != null">
	           and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z 존재 (시작일, 종료일 범위 주어짐): x, y, z
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE == null">
	           and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, y, a 존재 (시작일, 종료일 범위 주어짐): x, y, a
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE != null">
	          and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z, a 존재 (시작일, 종료일 범위 주어짐): x, z, a
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE != null">
	            and((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z, a 존재 (시작일, 종료일 범위 주어짐): y, z, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE != null">
	            and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            AND auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z, a 존재 (시작일, 종료일 범위 주어짐): x, y, z, a
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE != null">
	           and((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
			<otherwise></otherwise>
	        주어진 날짜 없음: (모든 날짜 범위가 주어지지 않음)
	    </choose>
	    	)  
    		</when>
    		<when test="end == 'end'">
    			and s(ysdate > auc_finish_date
    			<choose>
	        x만 존재 (시작일만 주어짐): x
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y 존재 (시작일 범위 주어짐): x, y
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z 존재 (시작일, 종료일 범위 주어짐): x, z
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE == null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, a 존재 (시작일, 종료일 범위 주어짐): x, a
	        <when test="aucSS != null and aucSE == null and aucFS == null and aucFE != null">
	           and ((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y만 존재 (시작일만 주어짐): y
	        <when test="aucSS == null and aucSE != null and aucFS == null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z 존재 (시작일 범위 주어짐): y, z
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]>  to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        y, a 존재 (시작일 범위 주어짐): y, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE == null">
	           and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z만 존재 (종료일만 주어짐): z
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE == null">
	           and (auc_finish_date aucFS <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        z, a 존재 (시작일, 종료일 범위 주어짐): z, a
	        <when test="aucSS == null and aucSE == null and aucFS != null and aucFE != null">
	           and (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	        a만 존재 (종료일만 주어짐): a
	        <when test="aucSS == null and aucSE == null and aucFS == null and aucFE != null">
	           and (auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z 존재 (시작일, 종료일 범위 주어짐): x, y, z
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE == null">
	           and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            or (auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
	
	        x, y, a 존재 (시작일, 종료일 범위 주어짐): x, y, a
	        <when test="aucSS != null and aucSE != null and aucFS == null and aucFE != null">
	          and ((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND auc_finish_date <![CDATA[<=]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, z, a 존재 (시작일, 종료일 범위 주어짐): x, z, a
	        <when test="aucSS != null and aucSE == null and aucFS != null and aucFE != null">
	            and((auc_start_date <![CDATA[>=]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS'))
	            or auc_finish_date <![CDATA[>=]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        y, z, a 존재 (시작일, 종료일 범위 주어짐): y, z, a
	        <when test="aucSS == null and aucSE != null and aucFS != null and aucFE != null">
	            and (auc_start_date <![CDATA[>=]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
	            AND auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS'))
	        </when>
	
	        x, y, z, a 존재 (시작일, 종료일 범위 주어짐): x, y, z, a
	        <when test="aucSS != null and aucSE != null and aucFS != null and aucFE != null">
	           and((auc_start_date BETWEEN to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS'))
	            AND (auc_finish_date BETWEEN to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS') AND to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')))
	        </when>
			<otherwise></otherwise>
	        주어진 날짜 없음: (모든 날짜 범위가 주어지지 않음)
	    </choose>
	    	)  
    		</when>
    	</choose> -->

	
	
	<!-- <if test="scheduled != null">
			 or sysdate <![CDATA[<]]> auc_finish_date
			 
			 <if test="aucSS != null">
				and auc_start_date <![CDATA[>]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			<if test="aucSE != null">
				and auc_start_date <![CDATA[<]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			<if test="aucFS != null">
				and auc_finish_date <![CDATA[<]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			<if test="aucFE != null">
				and auc_finish_date <![CDATA[<]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			
		</if>
		
		<if test="ongoing != null">
			or sysdate between auc_start_date and auc_finish_date
			
			<if test="aucSS != null">
				and auc_start_date <![CDATA[>]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			<if test="aucSE != null">
				and auc_start_date <![CDATA[<]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			<if test="aucFS != null">
				and auc_finish_date <![CDATA[<]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			<if test="aucFE != null">
				and auc_finish_date <![CDATA[<]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
		
		</if>
		
		<if test="end != null">
			or sysdate <![CDATA[>]]> auc_finish_date
			<if test="aucSS != null">
				and auc_start_date <![CDATA[>]]> to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			<if test="aucSE != null">
				and auc_start_date <![CDATA[<]]> to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			<if test="aucFS != null">
				and auc_finish_date <![CDATA[<]]> to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
			<if test="aucFE != null">
				and auc_finish_date <![CDATA[<]]> to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
			</if>
		</if> -->	

	
	
	<!-- <select id="getAdminSearchList" resultType="Auction">
		select auc_no, auc_start_price, auc_finish_price, to_char(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') as auc_start_date, 
				to_char(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') as auc_finish_date, auc_mem_id, con_author, con_product, con_width, con_height,
				con_year, con_etc, att_rename
		from auction
		    join (select *
		    		from consignment 
		    			join attachment on (con_no = att_bno) 
		    		where att_category = 2 and att_fno = 1) on (auc_no = con_no)
		<if test="auctionStatus != null">
			<choose>
				<when test="auctionStatus == 'ongoing'">
					<![CDATA[
						where sysdate >= to_date(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') and
	        				sysdate <= to_date(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') 
	        		]]>
				</when>
				<when test="auctionStatus == 'scheduled'">
					<![CDATA[
						where sysdate < to_date(auc_start_date, 'YYYY-MM-DD HH24:MI:SS')
	        		]]>
	        	</when>
	        	<when test="auctionStatus == 'end'">
					<![CDATA[
						where sysdate > to_date(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') 
	        		]]>
	        	</when>
	        	<otherwise></otherwise>
			</choose>
			<choose>
				<when test="searchType == all">
					and con_product like '%' ||  #{content} || '%' or con_author like  '%' ||  #{content} || '%'
				</when>
				<when test="searchType == 'work'">
					and con_product like '%' ||  #{content} || '%'
				</when>
				<when test="searchType == 'author'">
					and con_author like  '%' ||  #{content} || '%'
				</when>
			</choose>
			<if test="aucSS != null">
				<![CDATA[
					and to_date(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') >= to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
				]]>
			</if>
			<if test="aucSE != null">
				<![CDATA[
					and to_date(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') <= to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
				]]>
			</if>
			<if test="aucFS != null">
				<![CDATA[
					and to_date(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') >= to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
				]]>
			</if>
			<if test="aucFE != null">
				<![CDATA[
					and to_date(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') <= to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
				]]>
			</if>
		</if>
		<if test="auctionStatus == null">
			<choose>
				<when test="searchType == all">
					and con_product like '%' ||  #{content} || '%' and con_author like  '%' ||  #{content} || '%'
				</when>
				<when test="searchType == 'work'">
					and con_product like '%' ||  #{content} || '%'
				</when>
				<when test="searchType == 'author'">
					and con_author like  '%' ||  #{content} || '%'
				</when>
			</choose>
			<if test="aucSS != null">
				<![CDATA[
					and to_date(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') >= to_date(#{aucSS}, 'YYYY-MM-DD HH24:MI:SS')
				]]>
			</if>
			<if test="aucSE != null">
				<![CDATA[
					and to_date(auc_start_date, 'YYYY-MM-DD HH24:MI:SS') <= to_date(#{aucSE}, 'YYYY-MM-DD HH24:MI:SS')
				]]>
			</if>
			<if test="aucFS != null">
				<![CDATA[
					and to_date(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') >= to_date(#{aucFS}, 'YYYY-MM-DD HH24:MI:SS')
				]]>
			</if>
			<if test="aucFE != null">
				<![CDATA[
					and to_date(auc_finish_date, 'YYYY-MM-DD HH24:MI:SS') <= to_date(#{aucFE}, 'YYYY-MM-DD HH24:MI:SS')
				]]>
			</if>
		</if>
	</select> -->
	
	
	<insert id="insertAuction">
		insert into auction values(auc_seq.nextval, #{aucStartPrice}, default, to_date(#{aucStartDate}, 'YYYY-MM-DD HH24:MI:SS'), to_date(#{aucFinishDate}, 'YYYY-MM-DD HH24:MI:SS'), #{aucNo}, null)
	</insert>
	
	<select id="getConsignmentInfo">
		select *
		from consignment
		where con_no = #{conNo}
	</select>
	
	
	<select id="getAttachment">
		select *
		from attachment
		where att_bno = #{conNo} and (att_fno = 1 or att_fno = 2 or att_fno = 3)
	</select>
	
	
	
	
	
	
</mapper>
